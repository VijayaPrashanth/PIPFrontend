import React from 'react';
import { Typography } from "@material-ui/core";

const Bill = ()=>{
    return(
        <Typography variant="h5">
            Bill Details
        </Typography>
    );
};

export default Bill;